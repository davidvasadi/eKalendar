///itt lesz a regisztráció fetche////
let regForm = document.getElementById('regForm'); //regisztrációs form 
let regBtn = document.querySelector('.regBtn'); // regisztráció gomb

//gombnyomásra fut le a regisztrációs infó átadása
regBtn.addEventListener('click', () => {
    let formData = new FormData(regForm); // ez itt csinál egy objectet az összes form inputból egyben.
        
})
